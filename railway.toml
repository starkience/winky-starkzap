[build]
buildCommand = "cd api && npm install && npx tsc"

[deploy]
startCommand = "cd api && node dist/server.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
